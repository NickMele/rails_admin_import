- if @results && @results[:success].present?
  .alert.alert-success.form-horizontal.denser
    %fieldset
      %legend
        %i.icon-chevron-right
        = @results[:success_message]
      %ul.control-group{style: 'display: none'}
        - @results[:success].each do |message|
          %li= message
- if @results && @results[:skipped].present?
  .alert.alert-warning.form-horizontal.denser
    %fieldset
      %legend
        %i.icon-chevron-down
        = @results[:skipped_message]
      %ul.control-group
        - @results[:skipped].each do |message|
          %li= message
- if @results && @results[:error].present?
  .alert.alert-danger.form-horizontal.denser
    %fieldset
      %legend
        %i.icon-chevron-down
        = @results[:error_message]
      %ul.control-group
        - @results[:error].each do |message|
          %li= message
